.flash-message{ class: "flash-message--#{type}", data: { controller: 'flash' } }
  .flash-message__text
    %p= message
  .flash-message__close
    %p= link_to '&times;'.html_safe, '#close', data: { action: 'click->flash#close' }
