.activity
  %header.activity__event
    %span.activity__subject
      = link_to activity.user do
        = activity.subject
    %span.activity__object
      = link_to_activity(activity) do
        = activity.action
        = activity.object
  - if activity.exist?
    %main.activity__content
      = render activity.item
