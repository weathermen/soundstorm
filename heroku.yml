# Configuration for building Soundstorm on Heroku. Used for review apps,
# the staging server, and the "Deploy to Heroku" button.
build:
  docker:
    web: Dockerfile
run:
  web: bin/rails server
  worker:
    image: web
    command:
      - bundle exec sidekiq -C config/sidekiq.yml
